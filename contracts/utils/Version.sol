pragma solidity ^0.6.0;

// Version implements the current version getter.
contract Version {
    // version reports the current version of the contract
    function version() {
        // version 0.1.0
        return "010";
    }
}